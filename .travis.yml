sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t kavyanaik/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true kavyanaik/react-test npm test

after_success:
  - docker build -t kavyanaik/multi-client ./client
  - docker build -t kavyanaik/multi-nginx ./nginx
  - docker build -t kavyanaik/multi-server ./server
  - docker build -t kavyanaik/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push kavyanaik/multi-client
  - docker push kavyanaik/multi-nginx
  - docker push kavyanaik/multi-server
  - docker push kavyanaik/multi-worker
